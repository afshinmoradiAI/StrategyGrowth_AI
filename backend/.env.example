ANTHROPIC_API_KEY=your-key-here
DEFAULT_MODEL=claude-sonnet-4-5
LOG_LEVEL=INFO
MAX_TOKENS=4096

# Set to a secret string in production. Empty = auth disabled (dev only).
APP_API_KEY=

# Optional: override SQLite location (defaults to backend/data/plans.db)
# DB_PATH=backend/data/plans.db
